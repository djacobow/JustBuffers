This test covers example of the basic operation of the tool.

A c program is built that writes out a Just Buffer,
then a python script reads and parses it.

A different python script writes out a Just Buffer,
then a c program reads and parses it.

